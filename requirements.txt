# Memosyne v2.0 依赖（重构版本）

# === 核心依赖（与 v1 一致）===
openai>=1.30.0
anthropic>=0.31.0
pandas>=2.0.0
jsonschema>=4.20.0
python-dotenv>=1.0.1
tqdm>=4.66.0

# === 新增：重构所需依赖 ===
pydantic>=2.5.0              # 数据验证
pydantic-settings>=2.0.0     # 配置管理

# === 可选：开发工具 ===
# pytest>=7.4.0              # 单元测试
# pytest-cov>=4.1.0          # 测试覆盖率
# mypy>=1.7.0                # 类型检查
# ruff>=0.1.0                # 代码检查和格式化
